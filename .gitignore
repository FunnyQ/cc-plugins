.claude/handoffs/
node_modules/

# cockpit runtime state (per-project meta + session logs)
.cockpit/
!packages/chronicle/skills/adr/references/fixtures/**/.cockpit/

# impeccable live mode (local dev artifacts)
.impeccable-live.json
.impeccable-live/

# impeccable per-directory hook cache (generated, not source)
**/.impeccable/hook.cache.json
.handoffs/
