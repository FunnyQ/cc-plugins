---
name: claude
description: Delegate to the claude CLI (delegate / review) - alias for /relay claude.
argument-hint: "<delegate|review> [task]"
---

Use the `relay:relay` skill to handle the following request, with backend fixed to `claude`:

claude $ARGUMENTS
