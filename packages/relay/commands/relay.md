---
name: relay
description: Delegate code review, task execution, and image generation to codex, opencode, or claude CLIs
argument-hint: "<codex|opencode|claude> <delegate|review|image> [task]"
---

Use the `relay:relay` skill to handle the following request:

$ARGUMENTS
